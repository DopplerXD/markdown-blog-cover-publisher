---
name: markdown-blog-cover-publisher
description: >-
  为本地静态博客或文档站中的新 Markdown 文章生成并发布总结性封面图。
  当用户提到 Markdown 博客、文档站、新建或新增 md 文章、文章头图/封面图、
  assets 图片目录，或要求在提交推送 Git 前生成图片时使用。扫描新增且未覆盖的
  文章，提炼主题，使用当前 Agent 可用的生图能力生成卡通技术风格封面，等待用户
  明确审批后再保存图片、修改 Markdown、提交或推送。不得绑定特定 Agent、模型或
  skill 运行时；没有生图能力时仅准备提示词并报告限制。
---

# Markdown 博客封面发布器

使用本 skill 为新 Markdown 文章生成可发布的博客头图。封面用于在缩略图尺寸下
快速传达文章主题，不替代文章本身，也不应试图复刻完整架构或全文内容。

## 安全与审批约束

将图片审批和 Git 发布视为两个独立步骤：

- 先在仓库外或临时目录生成并展示候选图，未经明确选择不得修改仓库。
- 对每张图说明文章路径、文章标题、拟保存路径、Markdown 引用和是否会提交/推送。
- 只有用户明确选择“保存并发布”时才执行提交和推送；用户只要求生成封面时，保存后
  先停止并询问是否进行 Git 操作。
- 用户选择重绘时，只调整一两个目标因素并重新生成；不得把未审批的图片复制进仓库。
- 用户选择取消或仅预览时，保持 Markdown、图片目录和 Git index 不变。
- 不得默认强制推送、修改历史、reset、clean、覆盖已有封面或 amend 现有提交。
- 只暂存本次获批文章和对应图片，保留用户其他未相关的工作区修改。
- 推送前检查分支、remote、工作区状态、引用路径和 staged 文件列表；遇到认证、hook、
  非快进或受保护分支错误时停止，不绕过检查。

若当前 Agent 没有生图能力，完成文章分析和提示词后停止，并说明需要支持文生图的
Agent，或让用户运行本目录中的独立 Python 脚本。不要假装已经生成图片。

## 触发和输入

将以下请求视为触发条件：

- “给新写的 md 文章生成博客头图。”
- “扫描博客仓库新增文章，生成封面，确认后提交并推送。”
- “push 前为新文档补 cover，图片放对应 assets。”
- “Preview covers for the new Markdown posts; I’ll decide which one to publish.”

优先使用用户明确给出的仓库路径；相对路径相对于当前工作目录解析。用户未提供路径
时，只在确认当前 Git worktree 确实是目标静态博客后使用它，不要扫描整个 home 目录
寻找同名仓库。

## 工作流

### 1. 只读检查仓库

先确认仓库根目录、当前分支、remote/upstream 和工作区状态。扫描以下候选：

1. 未跟踪的 .md/.markdown 文件；
2. 相对于 HEAD 新增的 Markdown 文件。

排除顶部已经有封面引用或 cover front matter 的文件。只有用户明确要求刷新时，
才处理普通修改过的文章。用户点名文件时，以点名范围为准，并报告已存在封面的文件。

读取每篇候选的 YAML front matter、标题、开头摘要、一级/二级标题以及技术关键词。
统计正文时去除 YAML front matter，但代码块和 Markdown 标记仍计入正文长度。不要把
整篇文章直接复制到图像提示词中。

选择图片目录时依次采用：

1. 用户明确指定的 assets 目录；
2. 文章所在目录或其上级最近的现有 assets/、static/images/ 或 public/assets/；
3. 文章旁新建的 assets/。

如果多个目录约定同样合理，先暂停并询问，不要把图片分散到多个目录。

### 2. 设计封面提示词

每篇未覆盖文章生成一个独立候选。围绕文章真实主题设计一个清晰的视觉隐喻，使用
以下结构组织提示词：

~~~text
用途：静态博客文章横版封面，16:9 构图
主题：用一句话概括“<简短文章标题>”
场景：从文章主题提炼的一个具体且易懂的技术场景或隐喻
主体：代表核心问题或机制的一个主要对象/动作
风格：彩色卡通 + 技术编辑插画，友好的 Q 版比例，线条清晰
构图：缩略图可读，一个视觉焦点，适量留白，不使用密集流程图
氛围：好奇、调试、突破、平静或高效，按文章主题选择
配色：与主题相关的 2–4 种主色
文字：最多 1–4 个简短关键词，也可以不放文字
限制：无水印、无微小不可读文字、无大段代码、无杂乱标签
避免：写实服务器机房、通用科技 stock 图、完整 UI 截图、过多面板和 logo
~~~

遵循以下创作规则：

- 默认使用卡通和技术结合的视觉语言，例如发光数据路径、友好机器、代码符号、
  电路、笔记本、云和抽象系统隐喻。
- 保持文字简短。标题较长时优先制作无文字封面，让文章标题和 Markdown alt 文本
  承担精确信息。
- 只有文章或用户明确需要时，才加入一个或两个原创 Q 版虚拟 AI 角色互动。可以把
  Codex、Claude、DeepSeek、GLM、MiniMax、Qwen 作为文字或角色设定，但不要复制官方
  logo、受版权保护的角色画风，或暗示品牌背书。
- 使用一个主要隐喻和少量角色，让封面在一眼之内表达一个核心概念。

调用当前 Agent 暴露的任意栅格图像生成能力。不要假设一定存在 Codex、image2、
image_gen 或某个厂商模型。新封面使用文生图；用户提供本地参考图时先查看并标注
为风格/参考输入，除非用户明确要求修改，否则不要把它当作编辑目标。

### 3. 展示预览并等待选择

将候选图以可辨认的实际预览尺寸展示，并逐篇或批量询问用户。至少说明：

- 文章路径和推断标题；
- 拟保存的图片路径和 Markdown 相对引用；
- 使用的简短提示词/关键词；
- 接受后会执行保存、提交还是推送。

当结构化选择工具可用时使用它；否则用自然语言询问并等待明确答复。不能把“看起来
不错”、沉默或与审批无关的回复解释为允许推送。

### 4. 只安装获批封面

获批后将图片复制到选定 assets 目录，按文章 slug 或标题生成稳定且不冲突的文件名，
例如 kv-cache-selection-cover.png。已有文件不得直接覆盖；只有用户明确批准替换时
才使用版本后缀并保留旧文件可恢复。

在文章首个实际内容位置插入图片引用：

- 有 YAML front matter 时，放在结束 --- 后、首个标题前；
- 没有 front matter 时，放在文件第一行；
- 使用从文章到图片的相对 Markdown 路径；
- 使用简短且有意义的 alt 文本；
- 检查顶部是否已有图片或 cover 字段，避免重复插入。

### 5. Git 操作前验证

执行并检查以下内容：

~~~bash
git -C <repo-path> diff --check
git -C <repo-path> diff -- <article.md>
git -C <repo-path> status --short
~~~

确认图片真实存在、相对路径正确、YAML front matter 未损坏、没有重复封面，并且待提交
列表不含无关文件。发现问题时先修复，不要推送损坏的引用。

### 6. 获得授权后提交和推送

用户选择“保存并发布”且请求包含提交/推送时：

1. 只暂存获批 Markdown 文件和封面图片；
2. 展示精确的 staged 文件列表和提交信息；
3. 使用简洁提交信息，例如 feat(content): add cover for <article-slug>；
4. 将当前分支推送到已有 upstream，不使用 force；
5. 没有 upstream 时，先展示 remote 和分支，询问后再使用 git push -u；
6. 报告提交 hash、推送分支、文章路径和图片路径。

## 独立 Python 脚本

skill 系统不可用或希望完全在终端执行时，运行本目录的单脚本：

~~~bash
python3 blog_cover_workflow.py
~~~

先编辑 blog_cover_workflow.py 顶部的配置，设置 REPO_PATH、TEMP_IMAGE_DIR、
FINAL_IMAGE_ROOT、模型名、接口、API Key 和图片尺寸。脚本默认使用 DashScope 兼容
接口：摘要模型为 qwen3.5-flash，图像模型为 qwen-image-3.0。使用其他服务商时，
按其请求体、响应路径、headers 和脚本中的 payload 构造函数调整配置。

脚本会先批量生成全部候选图，将图片以
<markdown-stem>-cover-YYMMDDhhmmss.png 或 .jpg 的格式平铺保存到临时目录。时间戳
取收到图片模型结果的时间。临时图不会进入正式仓库；获批后默认复制到：

~~~text
<article-directory>/assets/<article-title>/<candidate-filename>
~~~

终端审批数字含义如下：

| 输入 | 操作 |
| --- | --- |
| 1 | 保存、提交并推送 |
| 2 | 保存并提交 |
| 3 | 仅保存 |
| 4 | 重绘当前文章，回车后输入新的提示指导 |
| 5 | 跳过当前文章，保留临时图并继续 |
| 6 | 停止脚本，保留此前结果和临时图 |
| 7 | 停止并回滚本次已经产生的仓库操作 |

重绘时复用已生成的文章摘要，只追加用户的新指导，不重新发送完整长 Markdown。回滚
通过补偿性 Git revert 撤销本次已创建的提交，不强推、不重写历史。单篇接口失败时记录
错误并继续处理其他文章。

### 长 Markdown 摘要分流

脚本顶部硬编码以下控制项：

~~~python
ENABLE_QWEN_SUMMARY = True
QWEN_SUMMARY_MODEL = "qwen3.5-flash"
QWEN_SUMMARY_MAX_CHARS = 1200
LONG_MARKDOWN_THRESHOLD = 1000
~~~

当去除 YAML front matter 后的正文长度大于 1000 个 Unicode 字符且开关启用时：

1. 将完整正文只发送给 QWEN_SUMMARY_MODEL；
2. 要求模型提炼主题、核心问题、关键机制、技术关键词和适合封面设计的视觉摘要；
3. 要求只返回总结正文，不输出 Markdown 代码块或包装文字，长度不超过 1200 字；
4. 在本地再次截断，确保摘要严格不超过 1200 个字符；
5. 图像模型只接收文章标题、标题列表、有限元信息和摘要，不接收完整长 Markdown。

正文长度为 1000 或更少时不调用摘要模型。摘要开关关闭、API Key 缺失、返回为空或
请求失败时，使用由标题、标题列表和截断正文组成的本地摘要；即使失败也不得把完整
长 Markdown 转发给图像模型。图像提示词还必须经过最终长度限制。

## 完成报告

结束时简洁报告：

- 已接受、重绘、取消、跳过的封面；
- 每张已接受封面的最终图片路径和 Markdown 引用；
- 文章是仅编辑、已提交还是已推送；
- 推送成功时的提交 hash 和分支；
- 已有封面而跳过的文章，以及因图片目录约定不明确而暂停的候选。

## 常见变体

### 仅预览

只执行仓库检查、提示词设计和生图预览。不要复制图片、编辑 Markdown、提交或推送。

### 刷新已有封面

仅在用户明确指定文章并批准替换时执行。保留旧图片，创建带版本号的新文件，明确更新
引用，并在提交前展示差异。

### 多篇新文章

扫描所有候选，为每篇生成独立提示词和图片，使用编号列表展示结果，并允许用户只接受
其中一部分。相关变更可以合并为一次提交，但必须报告每篇文章的最终结果。
